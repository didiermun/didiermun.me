const config = {
  mode: "jit",
  purge: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    fontFamily: {
      para: 'Work Sans'
    },
    extend: {},
  },

  plugins: [],
};

module.exports = config;

